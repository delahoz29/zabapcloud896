@EndUserText.label: 'Literal Access'
@MappingRole: true
define role ZAC_896_01 {
    grant
        select
            on
                ZI_CDS_32_896
                    where
                        CountryCode = 'ES';
    grant
        select
            on
                ZI_CDS_33_896 
                    where
                        Status = 'N';            
                        
}